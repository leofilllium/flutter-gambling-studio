# Route-retention diagnostic review

Evidence: in Flutter3.44.5 profile Web, 30 identical Settings/back cycles increased post-GC heap from about20 to49MB while DOM/listener counts remained constant. Strong GC paths retained exactly30 disposed route states via SemanticsOwner traversal map keys. Material Slider and the Back icon Tooltip supplied distinct OverlayPortal paths. Slider-only replacement left retention. Removing both paths while preserving accessibility yielded disposed Settings/slider/portal counts0→0 and PageRouteBuilder3→3. Remaining growth was dominated by compiled-code metadata; final exact-source/warm-tail recheck remained the application finalizer's task at observation time.

This addition strengthens the existing P9 investigation without changing its result table, severity, pass criterion or any P1–P10 gate. It prescribes equivalent workload, profile confirmation, before/after strong roots, all-owner verification and preserved semantics. It does not ban Material sliders/tooltips globally or claim every heap delta is a leak. The observed workaround remains version-specific application evidence, not a framework patch or copied game code.

Validation compares an additive-only paragraph against remote base, checks unchanged frontmatter and P1–P10 rows, and confirms required retaining-path/exact-build/accessibility/warmed-tail language. Manual review confirms no RNG, balance, compliance, visual-quality or authorization rule is weakened. The source report explicitly distinguishes the combined mitigation snapshots from later tiny semantics/border refinements; this proposal does not claim an unrun final-source check.
