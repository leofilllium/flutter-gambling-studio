# Learning proposal: Add final-size store crop inspection

Status: proposed; human review and merge required.

## Observed problem

The first strict panorama gate passed while two required store-only ball labels were clipped by publisher crops and the hero crossed a seam.

## Proposed improvement

Document a final-size contact-sheet checkpoint for protected labels and seam-adjacent hero before packaging, preserving strict numeric gates.

Base commit: `4b2e25938c0b221da638a0d699b73de83ab78730`

## Source evidence

### store-learning-evidence.txt

SHA-256: `e2efbb97d412555b63b4a0b027bad6e3f8654ca751dbec6b97da0206614a43c4`

    Observation: the first strict triptych composition passed the numeric art gate, but visual inspection of the delivered panels showed store-only x5 and x100 balls cut by the publisher crops, while the hero crossed a seam and the upper background was visually dense.

    Reproduction: compose the generated key art into three 1320x2868 panels with the first approved bounds and inspect the panel contact sheet at delivery size. Numeric gate output passed, but the contact sheet showed the x5/x100 inscriptions clipped. A targeted imagegen edit moving those two balls inward, followed by a strict recomposition, produced readable x5/x10 in panel 1 and x50/x100 in panel 3; the final carousel also showed x25 in panel 2.

    Improvement: add a reusable store-screenshot workflow checkpoint that requires a final-size contact-sheet inspection of every protected store-only label and seam-adjacent hero before packaging, even after the numeric art gate passes. This preserves the existing strict gate and catches crop/readability failures that metrics alone do not detect.

## Validation

- Command: `["python3", "-B", "-c", "from pathlib import Path; p=Path('.claude/skills/store-screenshots/SKILL.md'); s=p.read_text(); assert 'Make a final-size contact sheet' in s; assert 'rerun the strict composition gate' in s; print('store crop checkpoint present')"]`; exit 0; 2026-09-23T18:14:50.623528+00:00
  Output SHA-256: `736b1b9e23904e62aa6e66787c307e57a788d81b97377a56e0604c79d52d3abe`

    store crop checkpoint present

