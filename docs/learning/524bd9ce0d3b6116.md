# Learning proposal: Pin compatible flame_svg in autocreate

Status: proposed; human review and merge required.

## Observed problem

The autocreate runbook names flame_svg without the existing compatible version, allowing an incompatible dependency choice with Flame 1.18.x.

## Proposed improvement

Reference the canonical flame_svg ^1.10.0 constraint directly in the autocreate scaffold instructions and validate dependency resolution.

Base commit: `d4230a10eb32ad5827b58429227c0e2bc72e420a`

## Source evidence

### flame-svg-evidence.txt

SHA-256: `10b63290cc90821dbf35c6104062ef5d273adb7ff000b4d8fe1b1658a2874855`

    Observation (2026-09-16): During /autocreate scaffold setup on Flutter 3.27.0 / Dart 3.6.0, the unversioned `flame_svg` dependency was interpreted as `^1.11.0` while Flame was pinned to `1.18.0`.

    Reproduction:
    1. pubspec.yaml contained `flame: 1.18.0` and `flame_svg: ^1.11.0`.
    2. `flutter pub get` failed during version solving.

    Sanitized solver result:
    `flame_svg >=1.11.0` requires Flame `^1.19.0` or newer. The solver recommended the compatible 1.10.x line.

    Existing studio guidance already lists `flame_svg ^1.10.0` in README.md and `.claude/agents/technical-director.md`. The autocreate scaffold line names `flame_svg` without the compatible version.

    Proposed bounded remedy: cite the canonical `flame_svg ^1.10.0` constraint directly in `.claude/skills/autocreate/SKILL.md`. Validate its consistency and resolve a temporary pubspec with Flame 1.18.0 on the studio Flutter toolchain.

## Validation

- Command: `["python3", "-c", "from pathlib import Path; import subprocess,tempfile,shutil; text=Path('.claude/skills/autocreate/SKILL.md').read_text(); assert text.startswith('---\\nname: autocreate\\ndescription:'); assert text.count('---') >= 2; assert 'flame_svg ^1.10.0' in text; readme=Path('README.md').read_text(); assert 'flame_svg ^1.10.0' in readme; d=tempfile.mkdtemp(prefix='flame-svg-check-'); p=Path(d); (p/'pubspec.yaml').write_text('name: dependency_check\\nenvironment:\\n  sdk: ^3.6.0\\ndependencies:\\n  flutter:\\n    sdk: flutter\\n  flame: 1.18.0\\n  flame_svg: ^1.10.0\\n'); result=subprocess.run(['flutter','pub','get'],cwd=d,text=True,stdout=subprocess.PIPE,stderr=subprocess.STDOUT); assert result.returncode == 0, result.stdout; lock=(p/'pubspec.lock').read_text(); assert 'flame_svg:' in lock and 'flame:' in lock; shutil.rmtree(d); print('PASS: skill structure, canonical reference, and real dependency resolution verified')"]`; exit 0; 2026-09-16T11:30:08.209795+00:00
  Output SHA-256: `2864e8f291f9e39cac4ae54f67b3bc00eac936127565fbf487c076509fed0775`

    PASS: skill structure, canonical reference, and real dependency resolution verified

