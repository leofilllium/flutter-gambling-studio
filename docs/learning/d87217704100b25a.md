# Learning proposal: Make cutout alpha checks directory-aware

Status: proposed; human review and merge required.

## Observed problem

cutout.py check treated a sprite directory as a file during store asset validation, requiring manual enumeration.

## Proposed improvement

Make the validation path explicitly accept directories or provide a canonical per-file iteration without weakening alpha checks.

Base commit: `3d5856a5bcf4e411833f706570e6ac0e6035be90`

## Source evidence

### store-screenshots-learning-evidence.txt

SHA-256: `d8c1c8c621aea00462e017e94e76bc459e7e0aa4bba776f4e7eb6c0a9bc6a9ef`

    Observation: During /store-screenshots alpha validation, `python3 tools/cutout.py --check <sprite-directory>` failed because the helper interpreted the directory as a file. The bounded correction was to enumerate PNG files and invoke `--check` once per file; all eight shipped sprite checks then passed.
    Scope: asset alpha verification in the store-screenshot workflow.
    Evidence: directory invocation returned a file-open error; per-file loop completed eight clean checks. No generated game or runtime files were changed.
    Proposed remedy: improve the cutout helper's check-mode error or document/accept directory iteration in the canonical store-screenshot validation path, preserving the existing alpha checks.

## Validation

- Command: `["python3", "-B", "-m", "unittest", "tools.tests.test_cutout", "-v"]`; exit 0; 2026-09-23T14:06:05.591115+00:00
  Output SHA-256: `42181143347860b431da8b01a1fcdee59270c75aad1dc165221f116cd1415797`

    test_trailing_barrier_does_not_index_past_hit_table (tools.tests.test_cutout.FloodFillTests.test_trailing_barrier_does_not_index_past_hit_table) ... ok
    test_missing_directory_is_retained_for_file_not_found_reporting (tools.tests.test_cutout.InputCollectionTests.test_missing_directory_is_retained_for_file_not_found_reporting) ... ok
    test_positional_directory_expands_sorted_png_files (tools.tests.test_cutout.InputCollectionTests.test_positional_directory_expands_sorted_png_files) ... ok

    ----------------------------------------------------------------------
    Ran 3 tests in 0.021s

    OK

