# Learning proposal: Repair Session2 references after autocreate router split

Status: proposed; human review and merge required.

## Observed problem

The implementation runbook points to detailed Phases4-10 and10.7 in autocreate, but autocreate is now a Session1 router without those sections.

## Proposed improvement

Make autocreate-implement the explicit owner of its phase protocol and handoff instructions, replacing broken canonical references without removing quality gates.

Base commit: `15445729f6dea385c4a6edb525fd49ffc77bc17b`

## Source evidence

### runbook-gap-evidence.txt

SHA-256: `fd933afce612b49c8bed24902b8de7d8e35db9a9c0e2458de97dceab8b945adb`

    During Session 2 preflight, autocreate-implement/SKILL.md mandatory contract item 5 and Phases4-10 section instructed the agent to execute detailed canonical phases in autocreate/SKILL.md.
    Inspection of autocreate/SKILL.md found a 220-line Session1/router document with Phase3.8 handing off to Session2, but no detailed Phase4 implementation, Phase5 integration or Phase10 crash checklist.
    Session2 correctly continued from autocreate-implement's explicit sequential role map, quality-gate table and persona briefs, preserving all gates.
    The same implementation skill references Phase10.7 in autocreate for handoff details that no longer exist there. Its own Phase10.7 paragraph contains sufficient routing instructions.
    Proposed remedy: make autocreate-implement own its phase descriptions and remove stale canonical references; preserve all stated quality gates and handoff requirements.

## Validation

- Command: `["/Users/leofillium/Documents/joker-game/.venv/bin/python", "-B", "-c", "from pathlib import Path; import subprocess,yaml; p=Path('.claude/skills/autocreate-implement/SKILL.md'); s=p.read_text(); old=subprocess.check_output(['git','show','HEAD:'+str(p)],text=True); front=lambda x:yaml.safe_load(x.split('---',2)[1]); assert front(s)==front(old); assert front(s)['name']=='autocreate-implement'; markers=['18 connections','20/20','15+ screens','100+ checks','A \u2192 E \u2192 D \u2192 B \u2192 C','autocreate-handoff.md']; assert all(x in s for x in markers); assert 'Phases 4\u201310 run in Session 2' not in s; assert 'You are Session 3 of /autocreate.' in s; assert all(Path(x).is_file() for x in ['.claude/skills/autocreate-finalize/SKILL.md','.claude/agents/lead-programmer.md','.claude/docs/mobile-first-contract.md']); assert 'Do not build an AAB/APK' in s; print('PASS: valid unchanged repository frontmatter, repaired routing, preserved gates and existing downstream references')"]`; exit 0; 2026-09-13T16:50:38.903258+00:00
  Output SHA-256: `3dcefde5db1a173cfc659234887899d9b6d900459b552718722822ee175ba6f2`

    PASS: valid unchanged repository frontmatter, repaired routing, preserved gates and existing downstream references

