# Learning proposal: Make background reference guards order independent

Status: proposed; human review and merge required.

## Observed problem

Repeated rg reference inventories differed only in traversal order and falsely reported background wiring edits.

## Proposed improvement

Require stable sorting before comparing reference inventories while preserving all path and content checks.

Base commit: `6a55a706488b05f19ce743bbd882096d8f1f23e5`

## Source evidence

### store-guard-evidence.txt

SHA-256: `822b6cccb40bcff5bd539638743e002330750e812fc90786d27e00f9eeb11d8e`

    During the store run, exact comparison of two rg -n background-reference outputs failed although no background wiring was changed.
    The full unified diff contained only reordered lib/assets.dart and lib/game/joker_game.dart lines.
    Sorting both complete line lists made them equal, and every guarded background/splash SHA-256 remained identical.
    The check retained path, line number and content; it removed traversal-order nondeterminism only.
    A historical release verifier also rejected a stale pre-task baseline; that is expected baseline scope, not evidence of a runtime regression.

## Validation

- Command: `["python3", "-B", "-c", "from pathlib import Path; a=[\"lib/assets.dart:3:background=A\",\"lib/game.dart:4:background=B\"]; b=list(reversed(a)); same=lambda x,y:sorted(x)==sorted(y); assert a!=b and same(a,b); assert not same(a,[a[0],\"lib/game.dart:4:background=C\"]); assert not same(a,a+[a[0]]); assert not same(a,a[:1]); text=Path(\".claude/docs/visual-context.md\").read_text(); assert all(s in text for s in [\"Deterministic runtime-background guards\",\"Keep paths and complete matched content\",\"Preserve raw inventories\"]); print(\"PASS: reorder accepted; changed, added, removed and duplicate records detected; raw evidence preserved.\")"]`; exit 0; 2026-09-13T20:57:06.287529+00:00
  Output SHA-256: `79411b53bb5ea521fd3cc59697238ebd6be9dc3316ea36cc4653755c5ca4a095`

    PASS: reorder accepted; changed, added, removed and duplicate records detected; raw evidence preserved.

