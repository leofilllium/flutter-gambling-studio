# Bootstrap route audit review

The observed problem is a real non-root Web reload error in the temporary loading app, fixed by a navigator-free loading builder while the real router retains consent guards. Installed Flutter3.44.5 gives a non-root platform route precedence over initialRoute. Actual reload then preserved the same saved profile and logged no error. The proposed C1 row expands the existing routing audit to include startup states rather than inventing a new workflow or requiring every app to discard deep links.

Evidence precision correction: the immutable helper source snapshot was recorded with an inaccurate phrase saying the new regression covers failure/retry. The actual test/screens/bootstrap_route_test.dart has two cases: loading to ready with consent accepted and unaccepted. The error/retry UI uses the same navigator-free builder by source inspection; failed bootstrap is not separately exercised by that new regression. The local evidence file was corrected before publication. This review is authoritative for the test scope; no failed-loading runtime test is claimed. Testing the failure/retry phase in future audits is a recommendation justified by its shared router ownership, not a reported completed test.

Structured validation compares the single changed C1 row against remote base, preserves all other lines and unchanged frontmatter, verifies the startup/platform/consent coverage, and reads this explicit evidence correction. Source rules for RNG/math/compliance/mobile geometry and other gates remain unchanged. The helper report plus this review are the full proposal evidence.
