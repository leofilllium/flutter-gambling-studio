# Learning proposal: Require explicit multiplier seam visibility review

Status: proposed; human review and merge required.

## Observed problem

Final store crop review found a generated multiplier ball aligned with a publisher seam even though the strict geometry gate passed; the Play run also retained a hero seam blocker after the bounded correction.

## Proposed improvement

Add a concise final-panel multiplier-inscription and seam-adjacency checklist to the store-screenshots runbook, tied to the existing brief and contact-sheet review, without weakening the strict art gate.

Base commit: `abb07c1d2916b2c6e1de230964af6a2c4056e951`

## Source evidence

### store-seam-learning.txt

SHA-256: `47e8cd5b84210a02326369b7a67b42aa2830a8f52997ae0051d8557ea5204383`

    Observation: In the fresh Joker Jewels store run, the compositor's strict App Store export passed, but final-size inspection showed the store-only x50 multiplier ball aligned with the App Store panel 2/3 seam. The Play strict export also reported a structural hero seam blocker; a bounded measured correction did not remove the advisory, so the Play deliverable was kept as a warning-gate output and documented.

    Evidence: art/appstore-triptych-zoom.log reports a strict pass with a seam 2->3 allowance and the current panel contact sheet art/appstore-grid-final.jpg shows the x50 ball at the panel boundary. art/play-triptych-final.log reports the remaining hero silhouette seam blocker. STORE_INFO.md records both outcomes.

    Inference: the workflow already requires visual review, but the reusable check would be stronger if the brief/export checklist required explicit final-panel visibility mapping for every generated multiplier inscription and a hard seam-adjacency review before packaging.

## Validation

- Command: `["python3", "-B", "-c", "from pathlib import Path; s=Path('.claude/skills/store-screenshots/SKILL.md').read_text(); required=['final-panel ball map','x5','x10','x25','x50','x100','seam-adjacency']; assert all(x in s for x in required); print('store skill seam-review guidance verified')"]`; exit 0; 2026-09-24T19:38:12.681353+00:00
  Output SHA-256: `d663779456d105f8275572fb16bb0c1fa0f2cce1402a02e2c4b553620ad3dcdc`

    store skill seam-review guidance verified

