# Learning proposal: Plan store ball zones before panorama generation

Status: proposed; human review and merge required.

## Observed problem

Required store-only reward balls repeatedly crossed three-panel seams in the Play panorama, forcing several regeneration and crop retries.

## Proposed improvement

Add a pre-generation ball-map check to the store-screenshot workflow and recommend a format-specific panorama source when safe zones cannot hold all required balls.

Base commit: `2f6406d0999a022e42fdadfcb9a691b350d2fa58`

## Source evidence

### store_play_seam_learning.txt

SHA-256: `ba5b49e2661b1b98fa355c7e3df2bb806bd37c4de33f83eef49b4698a0eabf3c`

    Observation: During a fresh character-led store-screenshot run, the first wide Play panorama sources placed the required x25 reward ball across a panel seam and one generated source clipped x25 before composition. Repeated panorama renders and crop variants were needed before a dedicated Play source with x5 in panel 1, x10/x25 in panel 2, and x50/x100 in panel 3 produced readable labels.
    Reproduction: Compose a 1536x1024 source containing five required reward balls with the shared three-panel crop workflow at 1080x1920 per panel; inspect the carousel preview. A ball centered too close to a 1/3 seam is split or clipped.
    Reusable improvement: Before generating each panorama format, require an explicit per-panel ball map with normalized safe-center zones and route a format-specific source when the requested five balls cannot fit those zones. Preserve the existing art and compliance gates.
    Scope: Character-led three-panel store panoramas with required store-only reward balls. This does not change runtime gameplay or math.

## Validation

- Command: `["python3", "-B", "-c", "from pathlib import Path; t=Path('.claude/skills/store-screenshots/SKILL.md').read_text(); assert 'format-specific ball map' in t; assert 'safe-center zones' in t; assert 'seam clip' in t; print('store-screenshots ball-map guidance present')"]`; exit 0; 2026-09-24T08:20:33.302714+00:00
  Output SHA-256: `404eb0543020d26b56bc7ccfdff536f685de6649f3e2ada69b88f07c42992c41`

    store-screenshots ball-map guidance present

